export interface ICreateStudent {
  // fullName: string;
  email: string;
  // phoneNumber: string;
  // university: string;
  // admissionYear: number;
  passwordHash: string;
  otpHash: string;
  otpLastSentAt: Date;
}
